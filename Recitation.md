Problem: 

sum <- 0
for i<-1 to n do
    sum <- sum + (i^2 - i)
return sum

Time Complexity?
Theta(n)


Problem:
```
k <- 0 
if(x>y) then:
    for(int i = 1; i<y; i++) //y
        for(int j = 1; j < x; j=j*2) //log x

```
Answer = O(ylogx)

Problem:
Suppose (ab)^2 = m
```
for (i<-1; i<= n; i<- i+n/a)
    for( j <- 1; j <= n^2; j <- j +n/b)
        do nothing
``` 

Time complextiy:

